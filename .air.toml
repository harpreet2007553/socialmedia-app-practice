[build]
  cmd = "go build -o backend-in-go.exe"
  bin = "backend-in-go.exe"
  include_ext = ["go"]
  exclude_dir = ["vendor"]
  delay = 1000
  stop_on_error = true

[log]
  time = true

[color]
  main = "yellow"
  watcher = "cyan"
  build = "green"
  runner = "magenta"
